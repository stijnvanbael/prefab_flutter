/// Signals that a prefab entity supports create / update operations.
///
/// When present on a class annotated with [@View], the generated detail screen
/// renders an **Edit** action in the AppBar.
class Update {
  const Update();
}
