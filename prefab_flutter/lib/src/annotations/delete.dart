/// Signals that a prefab entity supports deletion.
///
/// When present on a class annotated with [@View], the generated detail screen
/// renders a **Delete** action in the AppBar backed by a confirmation dialog.
class Delete {
  const Delete();
}
